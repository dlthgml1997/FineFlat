<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.ssafy.happyhouse.model.mapper.HouseDealMapper">

	<select id="getSido" resultType="houseDealDto">
		SELECT left(sido_code,2)
		sido_code, sido_name
		FROM sidocode
		ORDER BY sido_code
	</select>
	
	<select id="getGugunInSido" resultType="houseDealDto">
		SELECT left(gugun_code,5) gugun_code, gugun_name FROM guguncode 
		where left(gugun_code,2) = #{sido_code}
		ORDER BY gugun_code
	</select>

	<select id="getDongInGugun" resultType="houseDealDto">
		select right(dongcode,5) AS code, dong 
		from dongcode 
		where left(dongcode, 5) = #{gugun_code}
		order by dong
	</select>
	
	<select id="searchByDong" resultType="houseDto">
		SELECT no,dong,AptName,code,jibun 
		FROM houseinfo 
		WHERE dong like (select dong 
						from dongcode 
						where dongcode = #{totalcode})
		ORDER BY AptName
	</select>
	
	<select id="searchByDongName" resultType="houseDto">
		SELECT no,dong,AptName, buildYear,jibun 
		FROM houseinfo 
		WHERE dong like concat('%', #{searchWord}, '%')
		ORDER BY AptName
	</select>
</mapper>